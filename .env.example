# Copy to `.env` (gitignored) for debug config "Local preview (Google Sheet)".
# Get the URL from Apps Script: Deploy → Web app → copy …/exec
DIARY_REMOTE_WEBAPP_URL=https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
# PORT=3333
